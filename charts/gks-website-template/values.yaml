# Default values.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ""
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80

env: []
envFrom: []

tenant: ""

deploymentAnnotations: []
serviceAnnotations: []

volumes: []
volumeMounts: []

certificates: []
  #- name: my-certificate
  #  # Optional, defaults to .Release.Namespace
  #  namespace: my-namespace
  #  commonName: "my-test.getadigital.cloud"
  #  dnsNames:
  #    - "my-test.getadigital.cloud"
  #    - "*.my-test.getadigital.cloud"
  #  issuer: my-issuer
  #  secretName: my-cert-secret
  #  secretAnnotations:
  #    my-annotations: value

gateways: []
  #- name: my-gateway
  #  # Optional, defaults to .Release.Namespace
  #  namespace: my-namespace
  #  ingressGateway: my-ingress-gateway
  #  certificateSecret: my-cert-secret
  #  # Optional, defaults to $namespace/*
  #  hosts:
  #    # Items are transformed to $namespace/$host
  #    - "my-test.getadigital.cloud"
  #    - "*.my-test.getadigital.cloud"

routing:
  # web:
  #   endpoints:
  #     - name: my-endpoint
  #       hosts: 
  #         - "mydomain.net"
  #       gateway: my-gateway
  #       service:
  #         # Optional, default is 80
  #         port: 80
  #         host: mydomain-web
  #         # Optional, defaults to .Release.Namespace
  #         namespace: ns 
  # api:
  #   endpoints:
  #     - name: external
  #       host: api.mydomain.net
  #       gateway: my-gateway
  #       # Optional
  #       headers: {}
  #       # Optional
  #       cors:
  #         credentials: true
  #         origins: []
  #       routes:
  #         - 
  #           match:
  #             # Optional, defaults to /
  #             prefix: /cms/
  #             # Optional, defaults to /
  #             rewrite: /
  #           service:
  #             # Optional, default is 80
  #             port: 80
  #             host: my-api
  #             # Optional, defaults to .Release.Namespace
  #             namespace: ns
  #   # If global routes are specified - endpoint specific routing is skipped
  #   routes:
  #     - 
  #       match:
  #         # Optional, defaults to /
  #         prefix: /cms/
  #         # Optional, defaults to /
  #         rewrite: /
  #       service:
  #         # Optional, default is 80
  #         port: 80
  #         host: my-api
  #         # Optional, defaults to .Release.Namespace
  #         namespace: ns

probes:
  liveness: {}
  startup: {}
  readiness: {}

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}
