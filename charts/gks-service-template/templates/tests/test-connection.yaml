apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gcc.shared.fullname" . }}-test-connection"
  labels:
    {{- include "gcc.shared.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gcc.shared.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
